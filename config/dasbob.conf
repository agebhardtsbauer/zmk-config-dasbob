# Settings shared by all three dasbob boards (left, right, dongle).
# Role-specific overrides (USB, sleep) live in build.yaml cmake-args and in
# boards/shields/dasbob/dasbob_dongle.conf so they can't collide here.
